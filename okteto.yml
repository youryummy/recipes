build:
  recipes:
    context: .

deploy:
  - helm upgrade --install recipes chart
    --set image.repository=${OKTETO_BUILD_RECIPES_IMAGE}

dev:
  recipes:
    command: npm start
    sync:
      - /source/app
