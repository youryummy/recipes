build:
  recipes:
    context: .

deploy:
  - helm upgrade --install recipes chart
    --set recipes.image=${OKTETO_BUILD_RECIPES_IMAGE}
    --set recipesMongoUrl=$RECIPES_DB_URL

dev:
  recipes:
    command: npm start
    sync:
      - .:/app
